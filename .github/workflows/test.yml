---
name Test

on: [push, pull_request]

jobs:
  build:
    runs-on: windows-latest
  tests:
    name: Run unit tests
    runs-on: windows-latest
    steps:
    - uses: actions/checkout@v2.1.0
    - run: dotnet test